# FastAPI and Server
fastapi>=0.136.0
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32
Pillow>=10.4.0

# MongoDB
motor>=3.7.1,<3.8.0
pymongo>=4.17.0,<4.18.0
mongomock-motor>=0.0.36

# Redis
redis>=7.4.1

# Data Validation
pydantic>=2.13.4
pydantic-settings>=2.14.1
defusedxml>=0.7.1
typer>=0.24.1,<1

# Google Gen AI SDK (maintained Gemini SDK; do not use deprecated
# google-generativeai in production code).
google-genai>=1.0.0

# CORS and Security
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# Environment
python-dotenv>=1.2.2

# Logging and Error Tracking
loguru>=0.7.3
sentry-sdk[fastapi,loguru]>=2.20.0

# HTTP Client (for calling DL-Model-Support)
httpx>=0.28.1
aiohttp>=3.14.1

# Knowledge Graph (KuzuDB)
kuzu>=0.11.3

# Embeddings
sentence-transformers>=4.1.0,<5.0.0
numpy>=1.26.4,<2.0.0
sympy>=1.13.1

# STT / TTS
faster-whisper>=1.2.1
# Native streaming primary. Kept behind an optional runtime adapter so an
# unsupported wheel degrades to Faster-Whisper instead of crashing startup.
moonshine-voice>=0.0.59,<0.1
sherpa-onnx>=1.13.2,<2
piper-tts>=1.4.2
gTTS>=2.5.4

# HuBERT / Pronunciation Analysis (transformers will auto-install torch)
# NOTE: transformers>=5.0.0 requires torch>=2.4, which is NOT available on Intel Mac (x86_64).
# Pin to <5.0.0 to stay compatible with torch 2.2.x on Intel Mac.
transformers>=4.41.0,<5.0.0
accelerate>=0.34.2
# NOTE: torch>=2.4.0 has no wheels for macOS x86_64. Max available: 2.2.2.
torch>=2.2.0,<=2.2.2
peft>=0.19.1  # LoRA fine-tuning and adapter loading

# LangGraph + LangChain (TRACECAG Orchestration)
langgraph>=1.2.5
langchain-core>=1.4.7
langchain-community>=0.4.2

# LLaMA.cpp for GGUF model inference (Vietnamese explanation)
# Optional at runtime: the service already falls back when llama-cpp is absent.
# Restrict default install to x86_64, where this stack is currently expected.
llama-cpp-python>=0.3.28; platform_machine == "x86_64"

# Audio Processing (for HuBERT resampling)
scipy>=1.17.1

# HTTP Downloads
requests>=2.34.2

# System Monitoring (for ResourceManager)
psutil==7.2.2

# Development
black>=26.5.1
flake8>=7.3.0
mypy>=1.20.2
pytest==9.1.1
pytest-asyncio==1.4.0
networkx>=3.2.0,<3.3.0
gliner>=0.2.26

# Optional: Redis (if needed later)
# redis>=7.4.1
# aioredis>=2.0.1
pillow-heif>=0.16.0
