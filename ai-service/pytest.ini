[pytest]
# Only the test suite; top-level and scripts/ files named test_*.py are
# manual scripts that need live credentials.
testpaths = tests
markers =
    live_llm: makes real LLM API calls (costs money, non-deterministic). Skipped by default; set RUN_LIVE_LLM_TESTS=1 to run.
