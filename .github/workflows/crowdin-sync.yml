# Archived workflow: the former Flutter localization source/configuration is
# absent from `ai_tutor`. Keep this file for history, but do not run it until a
# reviewed Khmer glossary/localization pipeline and current Crowdin mapping exist.
name: Archived Crowdin Sync

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Archived: no current Crowdin source mapping is configured."'
