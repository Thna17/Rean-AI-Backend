# Project-specific ProGuard rules.
# Keep this file present because the Android Gradle pipeline references it
# during release minification (R8).

# Add custom keep rules here when introducing reflection-based libraries.
