# Environment Configuration for Course Crawler

# Note: Most free learning platforms don't require API keys
# The crawler uses publicly accessible web pages

# Proxy settings (optional - if you need to use a proxy)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
