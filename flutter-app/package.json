{
  "name": "lexilingo-flutter-app",
  "version": "1.0.0",
  "packageManager": "pnpm@11.1.0",
  "scripts": {
    "vercel-build": "test -f build/web/index.html && test -f build/web/assets/assets/env/prod_config || (echo 'Missing production build artifacts or public env bundle. Run flutter build web --release first.' && exit 1)",
    "deploy:prod": "flutter build web --release --no-tree-shake-icons && vercel build --prod && vercel deploy --prebuilt --prod"
  },
  "dependencies": {
    "vercel": "^54.9.1"
  }
}
