# Docker Compose for running AI Service locally
# This file is optimized for local development and hybrid deployment

version: '3.8'

services:
  # AI Service - runs on local machine
  ai-service:
    build:
      context: ./ai-service
      dockerfile: Dockerfile
    container_name: ai_tutor-ai-local
    ports:
      - "8001:8001"
    environment:
      # Gemini API Key for conversational AI
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      
      # Model cache directory
      - MODEL_CACHE_DIR=/app/models
      
      # Environment
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO

      # Dedicated AI Redis (isolated from other services)
      - REDIS_HOST=redis-ai
      - REDIS_PORT=6379
      - REDIS_DB=0
      - REDIS_URL=redis://redis-ai:6379/0
      
      # Performance tuning
      - WORKERS=1
      - TIMEOUT=300
      
    volumes:
      # Cache downloaded models to avoid re-downloading
      - ./ai-service/models:/app/models
      - ./ai-service/data:/app/data
      - ./logs:/app/logs
      
    restart: unless-stopped
    mem_limit: 2g
    memswap_limit: 2g
    
    # Resource limits (adjust based on your machine)
    deploy:
      resources:
        limits:
          cpus: '2.0'      # Use 2 CPU cores
          memory: 2G       # Max 2GB RAM
        reservations:
          cpus: '1.0'
          memory: 1G       # Reserve 1GB RAM
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    
    networks:
      - ai_tutor-network
    depends_on:
      redis-ai:
        condition: service_started

  # PostgreSQL for backend integration tests (ai_tutor_test DB)
  postgres-test:
    image: postgres:16-alpine
    container_name: ai_tutor-postgres-test
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ai_tutor
      POSTGRES_PASSWORD: ai_tutor_pass
      POSTGRES_DB: ai_tutor_test
    tmpfs:
      - /var/lib/postgresql/data   # in-memory → fast tests, no persistent state
    restart: "no"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_tutor -d ai_tutor_test"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - ai_tutor-network

  # Redis cache dedicated for AI
  redis-ai:
    image: redis:7-alpine
    container_name: ai_tutor-redis-ai-local
    ports:
      - "6381:6379"
    volumes:
      - redis-ai-data:/data
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lfu
    restart: unless-stopped
    mem_limit: 256m
    memswap_limit: 256m
    networks:
      - ai_tutor-network

networks:
  ai_tutor-network:
    driver: bridge

volumes:
  redis-ai-data:
    driver: local

# Usage:
# 1. Set GEMINI_API_KEY in .env file
# 2. Run: docker-compose -f docker-compose.local.yml up -d
# 3. Check logs: docker-compose -f docker-compose.local.yml logs -f ai-service
# 4. Stop: docker-compose -f docker-compose.local.yml down
